
import {ADD_CONTENT} from '../actions/index'

const init= {
  post:{
    title:'',
  content:'',
}
}
const reducer = (state=init, action) => {
  switch (action.type) {
    case ADD_CONTENT:
    return state,

  } return state,
}
export default reducer
